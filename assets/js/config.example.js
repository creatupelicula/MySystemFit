/* Copia este archivo como config.js y completa con tus credenciales
   de Supabase (Project Settings → API en supabase.com).
   config.js está en .gitignore — nunca subas tus claves al repo.
   La anon key es pública por diseño (la seguridad la da RLS). */
window.MSF_CONFIG = {
  SUPABASE_URL: "https://TU-PROYECTO.supabase.co",
  SUPABASE_ANON_KEY: "TU-ANON-KEY",
};
